# 政治知识点每日接续 ExecPlan

## 目标

把夸克政治主线目录对应的知识结构直接嵌入「今日学习」，让用户每天打开页面就获得一个明确的知识单元、核心知识点和对应肖1000题范围；完成合格训练后，下一学习日自动进入下一单元。

## 边界

- 只提取用户资料目录中的课程顺序、章节名称和题号映射，不复制、上传或嵌入网盘视频与讲义。
- 沿用现有 101 政治 75/90 分钟预算、正确率门槛、错因复盘和闭卷框架，不改变 AI 模型、网关、Supabase 表结构或其他三科任务。
- 当前资料中没有明确题号的科目只映射到肖1000对应章节；第四章疑似题号笔误明确提示以试题分册为准。
- 知识主线进度随既有整包学习状态同步；当天单元固定，只有完整通过政治合格条件才计为已完成，下一日再接续。

## 设计

- 建立马原、思修、史纲、毛中特、新思想 5 科共 53 个有序知识单元，每单元包含 3—5 个可复述知识点和对应练习范围。
- 今日政治卡片先展示当天单元，再展示训练证据表；完整知识树折叠嵌套在卡片内部，当前单元与已完成单元分别标记。
- 新用户从马原导论开始；已完成用户自动选择第一个未完成单元；全部完成后按日期进入循环复习。
- 政治完成条件新增“脱离页面复述知识点”确认，避免只填做题数据但未完成知识学习。

## 里程碑

- [x] 从夸克目录核对五科课程顺序与可见题号映射。
- [x] 实现 53 节知识树、今日单元和自动接续状态。
- [x] 完成持久化、合格门与桌面/移动端回归测试。
- [x] 完成浏览器视觉验收和本地提交。

## 验收条件

1. 新用户今日政治显示马原导论、四个核心知识点和肖1000导论题号。
2. 完成当前单元全部合格条件后记录进度；新学习日自动进入下一单元，当天不突然跳题。
3. 完整知识树包含 5 科 53 节，可折叠查看，且清楚标记今日、待学习和已完成状态。
4. 390px 与桌面端无横向溢出，知识点、题号、表单层级清楚。
5. JavaScript、自动验收和浏览器控制台检查全部通过。

# AI 讲解后台并发 ExecPlan

## 目标

让每张卡片的 AI 讲解成为独立后台任务。用户切换卡片、收起讲解面板或继续复习时，已开始的讲解继续生成；其他卡片可以同时开始生成。已保存讲解仍可按当前思考档位重新生成，并且只在新结果完整成功后覆盖旧内容。

## 边界

- 只调整卡片讲解、错题本行内讲解及对应面板状态。
- 主 AI 助教的翻译判分、写作批改等请求继续使用原来的单任务停止逻辑。
- 不改变模型、网关地址、Key、提示词和 Low / High / Max 默认值。
- 不持久化未完成流；刷新页面会结束浏览器中的后台任务，但已完整保存的讲解不受影响。

## 设计

- 使用 `cardKey` 管理独立运行时任务，每个任务拥有自己的 `AbortController`、重试计时器、阶段、文本缓冲和所选路由。
- 流式任务只在对应卡片面板挂载时绘制；面板不存在时继续接收并缓冲。
- 重新打开同一卡片时恢复当前进度；成功后写入既有 `mti_ai_explain` 缓存并触发云同步。
- 关闭按钮只收起面板，不停止后台任务；面板提供独立的“停止此任务”入口。
- 重新生成期间保留旧缓存；新结果成功后原子覆盖，失败或停止时旧结果继续可用。

## 里程碑

- [x] 审计全局 `aiBusy` / `aiAbort` / 重试状态及卡片入口。
- [x] 实现按卡片隔离的后台流式任务。
- [x] 更新抽背卡、错题本、重新生成和面板状态。
- [x] 覆盖两个并发任务、切卡恢复、单任务停止和失败不覆盖。
- [x] 完成桌面与 390px Browser 验收、静态检查和本地提交。

## 验收条件

1. 卡片 A 开始生成后切到卡片 B，B 可以立即开始生成，A 不被取消。
2. 回到 A 可看到进行中的流或完整已保存结果。
3. 停止 A 不影响 B；关闭任一面板不停止任务。
4. 已保存讲解可以重新生成；新结果成功后覆盖，失败或停止时保留旧结果。
5. 流式更新不抢用户滚动位置，桌面与 390px 无横向溢出或控件遮挡。

# 判分解析云端永久保存 ExecPlan

## 目标

把 211 词汇、357 词条和 448 名词解释的 AI 判分解析从“仅本机最近 180 条”升级为 Supabase 云端永久保存。保留本机缓存作为秒开和断网兜底，换设备或清除浏览器数据后，打开同一道题仍可从云端恢复。

## 边界

- 沿用项目现有 Supabase 项目，新建专用 `ai_judge_records` 表；不改模型、网关、Key、提示词、判分与 SRS 规则。
- 云端按题目独立记录，覆盖重判只覆盖该题的最新解析；失败或停止不覆盖旧解析。
- 本机仍只保留最近 180 条，云端记录不因本机 LRU 淘汰而删除。
- Supabase 离线时先保存在本机并自动重试；恢复解析不得重复学习统计或推进任务。

## 设计

- 每道题使用 `SB_ID + '_judge_' + 编码后的题目 key` 作为 `ai_judge_records.id`，独立保存题目 key、来源签名与完整判分记录。
- 数据库触发器拒绝较旧 `recorded_at` 覆盖较新记录，避免离线旧设备晚上传时倒退云端版本。
- 新解析先原子写入本机，再进入云端队列；队列批量 upsert，失败后保留并退避重试。
- 每次打开题目都按精确云端 id 核对最新版本：云端较新则更新本机，离线本机较新则补传云端。
- 首次启动把现有本机 180 条迁移到云端，不需要用户重新判分。
- 状态栏区分“本机已保存、同步中、永久保存成功、暂未同步”。

## 里程碑

- [x] 核对项目编号、Supabase 归属与恢复状态。
- [x] 实现逐题云端记录、迁移、拉取、重试和状态反馈。
- [x] 验证刷新恢复、跨本机缓存恢复、覆盖、失败保护与无统计副作用。
- [x] 完成桌面和 390px 浏览器验收、静态检查与本地提交。

## 验收条件

1. 新解析成功后明确显示“已永久保存到云端”。
2. 删除该题本机缓存或换独立浏览器状态后，打开同题可从云端恢复输入、判定和完整解析。
3. 覆盖重判成功后云端只保留最新解析；失败、停止、无效判定不覆盖旧解析。
4. Supabase 暂时不可用时本机结果不丢，恢复联网后自动补传。
5. 云端恢复不触发 `tdOnJudged`、`grade` 或学习反馈动画。

# AI 追问独立云端保存 ExecPlan

## 目标

修复“回答已生成，但保存失败”。主讲解和每条追问改为按卡片、按追问独立写入 Supabase；本机主讲解缺失、浏览器存储空间不足或不同设备同时追问时，都不能阻止追问永久保存。

## 边界

- 不改 AI 模型、网关、Key、提示词、思考档位和现有讲解/追问正文。
- 保留 `mti_ai_explain` 本机缓存作为秒开与离线兜底，但云端不再依赖整包覆盖。
- 沿用现有 `progress` 表和 RLS，不新增生产依赖；新增记录只使用 `ganlin770_expcard_*` 与 `ganlin770_expqa_*` 前缀。
- 兼容旧的 `ganlin770_exp` 整包记录，并迁移其中已有讲解与追问。

## 设计

- 主讲解一张卡一行；追问一条回答一行，追问记录携带卡片 key、独立 id、问题、回答、模型、档位与时间。
- 追问保存不再要求本机先存在主讲解；运行时主讲解只用于跨设备恢复时的上下文兜底。
- 本机写入失败时保留内存副本并继续云端写入；云端失败时退避重试，状态提示区分“本机已保存”“云端同步中”“云端永久保存”。
- 启动时合并旧整包、逐卡主讲解和独立追问，以 `u/t` 时间和追问 id 去重，不让旧数据覆盖新数据。

## 里程碑

- [x] 复查线上失败状态、旧 key 修复与当前 Supabase 数据。
- [x] 实现逐卡主讲解、逐条追问保存与兼容拉取。
- [x] 迁移已有云端讲解/追问并核对数量。
- [x] 覆盖本机缺失、重复题、离线重试与跨设备恢复。
- [x] 完成 Browser 页面回归、独立保存测试、静态检查和本地提交。

## 验收条件

1. 本机没有主讲解缓存时，当前生成的追问仍能显示“已永久保存到云端”。
2. 同一卡片连续提问两次，两条记录均保留且刷新后恢复。
3. 重复题目使用完整 `aiCardId` 隔离，不串到同名另一张卡。
4. 清除该卡本机解释缓存后，主讲解和全部追问可由逐条云端记录恢复。
5. 云端暂时不可用时已生成正文不丢，恢复联网后自动补传。

# 今日学习合格度重构 ExecPlan

> 历史阶段说明：本节完成了原专业课合格门；其“101另行安排”边界已被下方「完整每日学习内容 ExecPlan」取代，当前版本已纳入101有效训练证据。

## 目标

让「今日学习」回答两个不同问题：任务有没有做完，以及训练是否真正合格。每天必须有明确题源、可执行题干、首轮表现、输出分数和延迟复测证据，不能再把“AI 有返回内容”直接等同于完成或合格。

## 边界

- 不更换模型、网关、Key、全局题库和既有云端存储结构。
- 本页只判定 211/357/448 专业课训练；101 政治缺少本项目内的可评分题库，必须明确另行安排，不能用无证据打卡冒充合格。
- 保留真题之外的广覆盖训练，但准确标注“真题 / 回忆版 / 同型练习”，低质量参考译文不得作为自动评分锚点。
- 普通词条、名解页面继续保留原有 40 张新卡上限；只修正「今日学习」中的复习清零与新学 10 张队列。
- 不自动推送；本轮仅做本地可审查提交。

## 设计

- 四道合格门：题源门、过程门、结果门、保持门。8 项打卡仅代表“任务完成”，四门同时达标才显示“今日合格”。
- 到期复习只抽到期卡，新学只抽未学词条且最多补足当日目标；修复新卡首次正确跳过 1 天复习的问题。
- 翻译每日题排除机翻、替代文、缺原文、OCR 错乱等高风险条目；界面显示题源等级，评分提示词也按实际等级称呼。
- 写作每天给出完整题干、身份、对象、要点和字数；周二提纲与周六成文固定使用同一周母题。
- 词汇和词条记录首轮正确率；翻译与写作从 AI 结果提取得分并按 357≥70%、448应用文≥28/40、448作文≥42/60、211作文≥20/30 判定。
- 晚间复测覆盖当天词汇、词条和名解；达到 80% 才通过保持门，并将次日无提示复测列为最终掌握条件。

## 里程碑

- [x] 核对南昌大学官方科目范围、黄皮书真题与当前每日生成逻辑。
- [x] 实现题源筛选、完整写作题干和稳定周计划。
- [x] 实现队列分离、首轮统计、分数门槛与晚间复测。
- [x] 更新今日学习和 AI 特训界面的合格状态说明。
- [x] 完成静态检查、逻辑断言、桌面/移动端浏览器验收和本地提交。

## 验收条件

1. 「8/8」只显示“任务完成”，没有晚间复测或未达分数线时不得显示“今日合格”。
2. 今日复习队列不混入新卡；今日新学队列最多 10 张且不先塞入所有到期卡。
3. 自动生成的翻译任务不选中已标注机翻、替代或原文缺失的篇章，并在页面准确显示题源等级。
4. 每个写作任务都有完整题干；同一周周二提纲与周六全文题目一致。
5. 211 首轮、词条加练、357/448 输出和晚间复测均有可见门槛与真实结果，刷新后状态保留。
6. JavaScript 语法、针对性逻辑断言和桌面/390px 浏览器检查全部通过。

# 完整每日学习内容 ExecPlan

## 目标

把「今日学习」从专业课任务清单扩展为一套可直接照做的完整日课。页面必须明确今天四科分别学什么、投入多少净时间、交付什么可检查成果，以及怎样订正和进入次日复测。

## 边界

- 当前阶段采用 420 分钟净学习模板：101 政治 75 分钟、211 通用英语能力 75 分钟、357 翻译 120 分钟、448 百科写作 105 分钟、错题与间隔复测 45 分钟。这是备考方案，不是南昌大学官方规定。
- 101 只记录用户使用现有政治资料后的做题量、正确数与错因复盘；本项目不生成或冒充南大专属政治题。
- 南大 211 原题证据不足，因此不能称为南大固定题型；阅读、语言精确度仍标为 D 级能力训练，写作轮换接入黄皮书逐页核对的北理工、西北政法、辽宁大学回忆题，标为 C 级、非南大、原题照录。
- 357、448 优先使用黄皮书中的南大回忆题题面，但一律标 C 级；参考答案只用于校勘，不作为官方唯一答案。
- 不更换 AI 模型、网关、Key、云端表结构或全局题库，不新增生产依赖；本轮按用户明确授权提交并推送当前分支。

## 设计

- 顶部增加完整日课路线，按 101、211、357、448、晚间保持五个时间预算展示预计总时长和当天可检查产出。
- 新增 101 政治证据任务：当前阶段每天完成 20-30 道有效选择题，并记录正确数、错因与一个闭卷知识框架；只看课不算完成。
- 211 主任务按周轮换深度阅读、语言精确度、英语写作与周复盘；每天仍保留词汇/搭配和听说输出，三部分合计约 75 分钟。
- 357 每天明确拆成限时初译、逐句修订、错误分类、表达提取和最差句重译；没有订正单不得通过过程门。
- 448 每天 6 个跨领域名解，并按周一应用文提纲、周二作文提纲、周三论证段、周四完整应用文、周五反方回应、周六完整大作文、周日最低分重写稳定轮换。
- 晚间只重测当天真正练过或答错的 101/211/357/448内容，任务完成、训练合格、答案可信、次日掌握继续分开显示。

## 里程碑

- [x] 核对官方科目、黄皮书题面和当前日课缺口。
- [x] 实现 420 分钟日课总览与 101 有效训练证据。
- [x] 实现 211 周轮换、357 订正单和 448 稳定周计划。
- [x] 扩充合格门、持久化状态和自动验收。
- [x] 完成桌面/移动端浏览器验收、静态检查和本地提交。

## 验收条件

1. 页面首屏明确显示 420 分钟四科日课，不再让用户自行猜测每天做什么。
2. 101 未达到当日题量或没有错因/框架记录时，全天训练不得显示合格。
3. 211 每天都有词汇、一个可评分主任务和听说输出；主任务随星期稳定轮换并明确标注非南大原题。
4. 357 必须保存结构化订正单；仅生成一次 AI 批改不能视为完成订正。
5. 448 同周应用文和大作文的提纲、段落与全文分别围绕同一题，且完整题面始终可见。
6. JavaScript 语法、队列与日课逻辑断言、桌面端及 390px 浏览器检查全部通过。

# 今日学习执行台与资料映射 ExecPlan

## 目标

在上一轮“训练是否合格”的基础上，进一步解决“打开页面后先做什么、每一科的时间花在哪里、本周真题如何递进、任务完成到哪一步”四个问题。保留现有纸张、墨绿、米白视觉体系，不改 AI 网关、模型、题库答案和云端表结构。

## 设计约束

- 首屏必须同时给出当前最优先动作、当日 420 分钟分配和四科合格状态，不能让用户先浏览大段说明再寻找入口。
- 101、211、357、448 按上午、下午、晚上、收尾复测分组；每组标注时间预算、完成进度和可检查交付物。
- 211 的 75 分钟必须拆清为词汇 20 分钟、主任务 40 分钟、听说复述 15 分钟；357 的 120 分钟必须拆清为初译 60 分钟、订正 30 分钟、表达/重译 15 分钟、新词条 15 分钟。
- 本周路线只复用程序已经存在的任务生成器：南大回忆题题面继续标 C 级，通用派生训练和无法核实的参考答案继续标 D 级。
- 当前动作按钮只能进入现有真实训练流程或定位到真实证据表，不能新增无效按钮或用打卡代替评分。
- 桌面与移动端均不得出现横向溢出；表单输入、AI 后台任务、草稿与滚动位置不得因界面重排而回归。

## 里程碑

- [x] 记录现状截图、DOM 与控制台基线。
- [x] 实现首屏“下一步”执行入口和分时段任务分组。
- [x] 实现本周 211/448 训练路线与分钟拆分。
- [x] 补充状态、跳转、响应式与回归测试。
- [x] 完成桌面/390px 浏览器验收；提交、推送与远端部署核对见本轮发布记录。

## 验收条件

1. 新用户打开今日学习即可看到唯一明确的“下一步”，点击后进入训练或聚焦对应证据表。
2. 页面明确展示基础/强化期上午 150 分钟（冲刺期政治增加 15 分钟）、下午 120 分钟、晚上 105 分钟、收尾 45 分钟，以及每组完成数。
3. 同一周的 211 和 448 任务在周路线中可预览，今天高亮，并保留来源等级。
4. 原有 9 项完成门、四道合格门、四科次日复测和草稿保存逻辑全部通过原有测试。
5. 1280px 与 390px 页面无横向溢出、无重复 ID、无控制台运行错误，当前动作交互有可见状态变化。

# 101 政治线上课程与练题系统 ExecPlan

## 目标

把用户夸克网盘中已核对的 101 政治主线，接入既有 MTI 学习网站，形成一套按课程单元学习、按肖1000范围练习、记录原创章节自测与错题、再回到次日复测的私有学习系统。

## 边界

- 只保存用户资料的章节结构、相对路径、学习顺序和题号范围；不复制、上传、公开嵌入或分发夸克课程视频、讲义、肖1000 PDF 或其他受版权保护题面。
- 网站内的选择题明确标记为原创章节自测，不能冒充肖1000或考研政治真题；肖1000和历年真题仅作为用户自有实体书/网盘资料的练习入口与成绩记录。
- 不改 AI 模型、网关、Supabase 表结构、211/357/448 题库和既有学习进度规则，不新增生产依赖。
- 夸克课程入口只使用通用网盘首页；个人深链只允许用户自行保存在本机，不写入仓库或云端。

## 设计

- 新增“政治系统”入口，展示五科 53 节课程地图、当前单元、对应夸克目录、肖1000范围和完成状态。
- 每节提供一组基于知识点生成的原创单选/判断自测，支持作答、立即反馈、错题留存、重练和本机持久化。
- 真题/肖1000练习区记录题源、题号范围、完成题数、正确数、错因与闭卷框架；成绩同步回既有今日 101 证据，不让只看视频通过。
- 练习结束后将错题和复测提醒清晰呈现，当前课程单元与既有“今日学习”53节主线保持同一进度。

## 里程碑

- [x] 审计夸克政治素材覆盖、既有 53 节结构与今日学习数据边界。
- [x] 实现政治系统页面、课程映射和原创章节自测。
- [x] 实现错题持久化、肖1000/真题练习记录与今日学习同步。
- [x] 补充自动验收，完成桌面和移动端回归。

## 验收条件

1. 页面清楚标明五科 53 节、当前单元、夸克相对路径及肖1000练习范围，且不会暴露个人夸克链接。
2. 新用户从马原导论进入，至少可完成一组原创章节自测；作答后可看到正确/错误、解析和错题数，刷新后仍保留。
3. 用户录入肖1000/真题练习的题数、正确数、错因与框架后，今日 101 证据同步更新；分数未达标不能推进课程。
4. 原创自测、肖1000/真题记录和受版权保护的资料边界在页面中清楚区分。
5. 现有今日学习、AI、词条卡与桌面/390px 回归全部通过。

# 双模型 Max 路由 ExecPlan

## 目标与边界

- 新请求只允许手动选择 `deepseek-v4-flash` 或 `gpt-5.6-luna`，两者固定 `Max`；默认 DeepSeek，全站共享并记忆。
- 失败时只在这两个模型之间单次互备，不再把 Kimi、Terra 或 DeepSeek V4 Pro 用于新请求；旧模型名仍可在历史记录中显示。
- 每个任务启动时复制独立路由，切换模型不影响运行中的讲解、追问、批改或后台任务；保存实际执行模型与 `max`。
- 保持现有 Ganlin Gateway 地址和浏览器端 Gateway Key；禁止把任何上游密钥写入网站或仓库。

## 里程碑

- [x] 完成全站双模型选择器、配置迁移和刷新持久化。
- [x] 完成前台流式请求与后台生成的双向单次备用和完整错误显示。
- [x] 完成实际模型记录、历史兼容显示和连接检测。
- [x] 更新自动化测试，执行完整回归、密钥扫描与差异审计。
- [x] 只暂存本任务改动并创建本地提交，不推送。

## 验收条件

1. 两个选择项均固定显示 Max，选择后刷新仍保持，全站所有新任务读取同一选择。
2. DeepSeek 请求携带 `thinking.type=enabled` 与 `reasoning_effort=max`；Luna 请求携带 `reasoning_effort=max`。
3. 任一首选失败时只请求另一个模型；两者都失败时展示包含两次错误的完整信息。
4. 运行中任务不因切换而变更模型；保存记录显示真实执行模型及 Max。
5. 自动化回归通过，Git 差异中无密钥、无无关任务改动，并产生本地提交。
